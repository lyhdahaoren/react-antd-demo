const returnUrl = (val)=>{
  let obj = {};
  try {
    switch (val) {
      case 'development':
        break;
      case 'dev':
        break;
      case 'production':
        break;
      default:
    }
  } catch (e) {
  }
  return obj;
}

export default returnUrl;
